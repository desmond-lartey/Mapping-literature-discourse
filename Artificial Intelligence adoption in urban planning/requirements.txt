# AI adoption in urban planning governance - Lartey & Law (2025)
# Landscape and Urban Planning 258, 105337
#
#   pip install -r requirements.txt

pandas>=1.5,<3.1
numpy>=1.23,<3.0
scipy>=1.9
matplotlib>=3.6
seaborn>=0.12
networkx>=2.8
openpyxl>=3.0

# Optional. Only needed if you swap the centroid scatter in notebook 03 for a
# true polygon choropleth; the notebook runs without it.
# geopandas>=0.13

# Optional. Notebook 03 geocodes offline against data/reference/countries.csv.
# Install this only if you want to match the paper's Nominatim path, which needs
# network access and is rate limited to one request per second.
# geopy>=2.3

# Nothing here requires a GPU.
